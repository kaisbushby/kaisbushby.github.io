import * as React from 'react'
import { Link } from 'react-router-dom'
import "./Navbar.scss"

export const Navbar = () => {
    return(
        <nav className='navbar'>
            
        </nav>
    )
}